name: Deploy Landing to Vercel Production

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel Production Deploy
        run: curl -X POST "${{ secrets.VERCEL_DEPLOY_HOOK_LANDING }}"
